import { BackboneView } from "@/common/Bone";

export default class DashboardView extends BackboneView {
    constructor(options: any) {
        super(options);
    }

    render() {
        return "<div>Dashboard</div>";
    }
}
