<td>
    <div class="d-flex align-items-center">
        <i class="fa fa-hashtag mr-2 text-muted"></i>
        <%=nit%>
    </div>
</td>
<td>
    <div class="d-flex align-items-center">
        <i class="fa fa-building mr-2 text-muted"></i>
        <%=capitalize(razsoc)%>
    </div>
</td>
<td>
    <div class="d-flex align-items-center">
        <i class="fa fa-id-card mr-2 text-muted"></i>
        <%=cedrep%>
    </div>
</td>
<td>
    <div class="d-flex align-items-center">
        <i class="fa fa-user-tie mr-2 text-muted"></i>
        <%=capitalize(repleg)%>
    </div>
</td>
<td>
    <div class="d-flex align-items-center">
        <i class="fa fa-file-alt mr-2 text-muted"></i>
        <%=capitalize(concepto)%>
    </div>
</td>
<td>
    <div class="btn-group" role="group">
        <!-- Botón de Detalle (siempre visible) -->
        <button type="button" data-toggle="bt_detalle" class="btn btn-info btn-sm" data-code="<%=nit%>"
            data-cid="<%=id%>" title="Ver detalles de la cartera">
            <i class="fa fa-eye"></i>
        </button>

        <!-- Botones de Administración (solo para SuperAdmin) -->
        <?php if (Session::getData("SuperAdmin") == 1) { ?>
        <button type="button" data-toggle="bt_editar" class="btn btn-warning btn-sm" data-code="<%=nit%>"
            data-cid="<%=id%>" title="Editar cartera">
            <i class="fa fa-edit"></i>
        </button>

        <button type="button" data-toggle="bt_borrar" class="btn btn-danger btn-sm" data-code="<%=nit%>"
            data-cid="<%=id%>" title="Eliminar cartera">
            <i class="fa fa-trash"></i>
        </button>

        <!-- Botón adicional de Exportación -->
        <button type="button" data-toggle="bt_exportar" class="btn btn-success btn-sm" data-code="<%=nit%>"
            data-cid="<%=id%>" title="Exportar cartera">
            <i class="fa fa-download"></i>
        </button>
        <?php } ?>
    </div>
</td>